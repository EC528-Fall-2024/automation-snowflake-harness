databaseChangeLog:
  - changeSet:
      id: 1
      author: krish
      changes:
        - createTable:
            tableName: change
            columns:
              - column:
                  name: id
                  type: int
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: varchar(255)
                  constraints:
                    nullable: false
              # Add other columns as needed
